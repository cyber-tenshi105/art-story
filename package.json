{
  "name": "art-story-frontend",
  "version": "1.0.0",
  "description": "Frontend for Artist Story API",
  "scripts": {
    "build": "echo 'No build process needed for static site' && exit 0",
    "vercel-build": "echo 'No build process needed for static site' && exit 0",
    "dev": "echo 'Open index.html in browser'"
  }
}